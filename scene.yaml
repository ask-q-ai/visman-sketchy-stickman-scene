---
canvasLayer:
- id: body-render
  context: canvas
  type: tracked-stickman
  position:
    x: 120
    y: 60
  size:
    width: 560
    height: 500
  properties:
    data: "{{ @hub['render.body']?.data }}"
  style:
    strokeStyle: "#FFFFFF"
    lineWidth: 2
